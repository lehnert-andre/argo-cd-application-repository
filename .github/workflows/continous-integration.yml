name: Continous Integration

on:
  push:
    branches: [ main, feature/** ]

env:
  DOCKER_USER: ${{secrets.DOCKER_USER}}
  DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD}}
  DOCKER_REPO_NAME: ${{secrets.DOCKER_REPO_NAME}}
  
jobs:
  
  update-gitops-repo:
    runs-on: ubuntu-latest
      
    steps:
    - uses: actions/checkout@v2 
      with:
        repository: lehnert-andre/argo-cd-gitops-repository
        path: './argo-cd-gitops-repository'

    - uses: imranismail/setup-kustomize@v1
    - run: |
        cd argo-cd-gitops-repository/kustomize/app/env/dev
        kustomize edit set image alehnert/argo-cd-application-repository:${GITHUB_SHA}
        git add .
        git commit -m "Set `alehnert/argo-cd-application-repository` image tag to `${GITHUB_SHA}`"
        git push
  
